name = "cli-guidelines"
compatibility_date = "2026-05-15"

# Build the Hugo site, then deploy ./public as the Worker's static assets.
# `wrangler deploy` runs [build].command before uploading [assets].directory.
[build]
command = "bash build.sh"

[assets]
directory = "./public"
